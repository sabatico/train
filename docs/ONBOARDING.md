# ONBOARDING — Spell Quest: Living State & Cold-Start Brief

> **The first thing any human or agent reads.** It always reflects the *current* reality so anyone can cold-start without prior context. **Stale state here is a bug** — update it at the end of every act.
> **Last updated:** 2026-07-08 · **Last session:** Grew the word corpus 28→736 (sourced online + `scripts/` pipeline + `engine/phonics.py` difficulty scoring), wired difficulty-aware selection (easy→hard by mastery). Owner picked pre-generated OpenAI TTS for dictation (T-014) + "AI enriches, engine leads" (T-015/016). Cross-authored tests in progress. Prior: **Phase-1 core playable end-to-end** — wired the DeepSeek teacher (B8: kid-voice correction feedback, PII-safe, live-verified) and built the kid UI (B7: SPA shell + registry + 3 exercise renderers + correction overlay + reward + `speech.js` TTS), all on the adopted design tokens. Browser-verified the full daily loop reaches ⭐. Fixed a real reloader bug (Flask debug reloader restarts on data writes → mid-session wipe → disabled). 312 tests green; JS UI tests deferred (T-010). Prior: session layer + engine brain. Untracked teacher-methodology PDF in `docs/` still awaiting owner intent.
> **Earlier same day:** Built the session layer; the full deterministic engine brain — `skills.py`/`classifier.py`/`selector.py`/`rewards.py` (lead-authored per ADR-007/008/011) + expanded word banks to 3 (28 words); Sonnet cross-authored 103 tests → 185 total green, 100% coverage on every engine module. Lead caught + fixed 2 bugs pre-handoff (phoneme-aware phonetic detection; selector warm-up sizing) and 1 ADR-011 doc typo (level-curve formula) post-review. Next: exercise-contract schema (ADR-005) + session builder & API (B6), then agent module (B8) and UI (B7). Owner note: untracked teacher-methodology PDF in `docs/` still awaiting owner intent.

## 1. What this is (stable)
Spell Quest is a local web app that teaches spelling to the owner's 7-year-old daughter (dyslexic, dysgraphic, ADHD; ~1st-grade spelling level, entering 3rd grade). It runs Orton-Gillingham-style structured literacy as software: a 14-skill mastery model with decay ("rose of winds"), an adaptive selector targeting ~80% success, ~10 exercise types, a strict wrong-spelling correction routine, and an AI teacher-agent (DeepSeek) that plans sessions, explains errors in kid language, and keeps a teacher notebook. Gamified (stars, levels, creature collection, beat-your-own-record) with a radically simple dyslexia/ADHD-safe UI. Full product spec: `PLAN.md`.

## 2. Reading order (stable)
`CLAUDE.md` → this file → `docs/adr/README.md` → `docs/feature-catalog.md` → `docs/runner.md` → then per task: `PLAN.md` (pedagogy + product), `docs/architecture.md`, `DESIGN_BRIEF.md` (UI spec), `docs/sops/*`.

## 3. Architecture at a glance (stable-ish)
Browser (vanilla JS component registry + token CSS + browser TTS) ⇄ Flask API ⇄ `engine/` (deterministic: skills/selector/classifier/session/rewards) ⇄ `data/` (file JSON DB) · `agent/` (DeepSeek teacher: session planning, kid-voice feedback, memory notebook) sits behind the engine with graceful fallback when offline. See `docs/architecture.md`, ADR-001/002/003.

## 4. Current status (VOLATILE — keep current)
- **Deterministic engine COMPLETE & tested (100% cov each):** `store.py` (atomic file DB, invariant #1), `config.py` (tunable constants), `models.py` (record defaults), `skills.py` (mastery EMA + lazy decay + gates, ADR-007), `classifier.py` (alignment + 9-tag taxonomy, ADR-008), `selector.py` (60/30/10 plan, ADR-007), `rewards.py` (add-only economy, ADR-011). `app.py` Flask skeleton. 185 tests, 99% overall. Run: `.venv/bin/pytest`.
- **Word banks:** 736 words / 9 patterns, each with phonemes + a 1–5 difficulty (selector serves easy→hard). AI-enriched phrase/sentence/emoji (707/708). **Natural `sage`-voice dictation audio generated for all 733 words** (`static/audio/`, `gpt-4o-mini-tts`); `speech.js` plays clips, browser TTS fallback. Both owner decisions (enrichment + TTS) complete.
- **🎉 PHASE-1 CORE IS PLAYABLE END-TO-END** (browser-verified): home → teach card → exercises (`word_builder`/`letter_boxes`/`echo_dictation`) → correction overlay (shows the correct word + kid-voice why) → reward (⭐ + level). Full deterministic engine (store/config/models/skills/classifier/selector/rewards/contracts/session — 100% cov each) + `/api/session/*` + the DeepSeek teacher (kid-voice feedback, PII-safe, live-verified) + the vanilla-JS UI (SPA shell + registry + `speech.js` TTS). **312 automated tests, 99% cov**; JS UI tests deferred (T-010).
- **Run it:** `.venv/bin/python app.py` → http://127.0.0.1:5000/app (or the preview). Set `SPELLQUEST_AGENT_LIVE=1` in `.env` for live kid-voice feedback (default off = canned, deterministic).
- **Remaining Phase-1 polish (backlog):** parent dashboard + radar (T-003), remaining exercise types (T-001), `sentence_scribe` (T-002), word-bank expansion (T-005), home streak/reward polish (T-013), self-host Lexend (T-011), JS unit tests (T-010), agent plan-enrichment (T-012).
- ADRs 001–012 all Accepted. DeepSeek key live.
- **Claude Design handoff RECEIVED** → `design_handoff_spell_quest/` (tokens.css, components.css, 2 rendered `.dc.html` references, README). Covers ALL kid screens + the 10 exercise cards ("pink + yellow sunny", red-panda mascot). **Not covered:** parent screens P1/P2 + the §8 state set (runner B10). The `.dc.html` files are references — never copy their inline-styled markup.
- `.env` in place with the real `DEEPSEEK_API_KEY` (gitignored; `.env.example` committed). Key probe-verified 2026-07-07 (HTTP 200; `deepseek-chat` alias currently serves `deepseek-v4-flash`). TBD-001 closed — the agent module builds against the live API from the start.

## 5. Decided vs. open (VOLATILE)
- **Decided / locked:** ADR-001 (Flask + file-JSON DB, atomic writes), ADR-002 (DeepSeek as the teacher-agent LLM, engine-fallback contract), ADR-003 (UI stack: token CSS + vanilla JS, no framework), ADR-004 (v1 single-user with four scale seams kept clean — `student_id` everywhere, stateless API, tenancy-shaped routes, WebView-clean frontend). Product/pedagogy spec = `PLAN.md`; Stage-2 direction (multi-user service, ~10k users, billing, WebView apps + push) = `PLAN.md` §10, parked as TBD-005/006/007. Single build phase, AI wired from the start.
- **Proposed, awaiting lock (Stage-1 build contracts, ADR-005..012):** data model, exercise contract, mastery+selection algorithm, classifier taxonomy, session lifecycle, frontend architecture, rewards economy, agent integration. Review order + gates in `docs/adr/README.md` roadmap. The 3 pedagogy ADRs (008 tag taxonomy, 007 mastery numbers, 011 rewards) want owner sign-off; the rest are rubber-stamps. **No section-B code builds until its governing ADR is Accepted.**
- **Open questions:**
  1. Which browser will the child use? (determines the TTS voice to tune for) — owner.
  2. Child's UI language/accent for TTS (US/UK English) — owner.
  3. Does the owner want the parent dashboard password-gated from the child? — owner.
  4. Sign-off on the pedagogy-critical ADRs (007 mastery formula/constants, 008 error-tag taxonomy, 011 reward curve) — owner.

## 6. Active milestone (VOLATILE)
**Wave 1 — Foundation:** [now] lock Stage-1 ADRs 005–012 (the lowered-altitude build contracts) → token-lock → backend skeleton (storage, skill model, selector, classifier, session API) + UI shell + first three exercise types (`word_builder`, `letter_boxes`, `echo_dictation`) + DeepSeek agent module with engine fallback. **Gate:** each build item waits on its governing ADR being Accepted (map in `docs/adr/README.md`). Live tracker: `docs/runner.md`.

## 7. How to work here (stable)
- Setup: `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt` *(requirements.txt not created yet)*.
- Run: `flask --app app run --debug` → http://127.0.0.1:5000.
- Test: `pytest --cov` (coverage 80–100% on changed code; top of band for `engine/`).
- Secrets: `DEEPSEEK_API_KEY` in gitignored `.env` only.
- DoD: `CLAUDE.md` §✅. UI work: read `docs/sops/ui-development-guardrails.md` first. Gotcha: TTS voices differ per browser — verify dictation in the child's actual browser.

## 8. Session log (append-only, NEWEST FIRST)
- **2026-07-08 (act-021)** — Two follow-ups from owner testing. (1) The `bad`/`cat` teach mismatch **persisted on the demo** because the teach text was baked into `current.json` at session start (before the fix) and the session resumes → made the teach card **compute fresh** in `_public_state` from the focus skill (content fixes now apply to in-flight sessions; no stale bake). (2) **Read-button spam** fired parallel `/api/tts` calls → added a busy-guard + stop-current in `speech.js` (`speakText` ignores taps while a read is in flight; `speak` stops any current audio). Browser-verified: 8 rapid taps → **1** API call; teach text now consistent. Redeploying to srv.
- **2026-07-08 (act-020)** — Two owner requests. (1) **Bug:** teach card showed example word `bad` but the short_vowels rule text hardcoded `cat` — made the canned rule word-agnostic (`engine/contracts.py`). (2) **Feature:** a **read-aloud speak button on every screen** — `.sq-speak-fab` (fixed, top-right) reads the current screen's text + her inputs in the `sage` voice via a new cached `POST /api/tts` (arbitrary-text OpenAI TTS in `agent/voice.py`, shared with the audio generator; 503→browser-TTS fallback when unkeyed). Browser-verified (reads "Hi! Ready for today's quest?" → 200 audio/mpeg). Cross-authored voice/endpoint tests in progress; redeploy to srv with `OPENAI_API_KEY` so read-aloud works there.
- **2026-07-08 (act-019)** — Stood up a **Docker demo on the `srv` box** (192.168.1.111, x86_64) for LAN testing from another computer. Added `Dockerfile` (gunicorn, 1 worker + threads; word banks + audio baked in; no secrets), `.dockerignore`, `requirements-docker.txt`. Installed our SSH key on srv (user `isa`, key-based now); `isa` needs `sudo` for Docker so deploy runs via `sudo -S`. Built on srv (matches its amd64), running as container `spellquest` (`--restart unless-stopped`, volume `spellquest_data`→`/app/data/students` for progress persistence) on a random port. **Live + verified from the Mac:** `http://192.168.1.111:38990/app` (+ /parent, sessions, audio all 200). Agent in canned mode (no keys in the image). To redeploy: rebuild `~/spellquest` on srv. Docker files committed.
- **2026-07-08 (act-018)** — Owner reported `because.mp3` playing much louder. Measured loudness across all 733 clips (bundled ffmpeg via `imageio-ffmpeg`): a huge 84 dB peak spread + **7 broken near-silent clips** (crab, jam, pet, stay, burst, game, cat — transient TTS glitches, peak < -35 dB). Fixed both: regenerated the 7 broken clips, then built `scripts/normalize_audio.py` (EBU R128 loudnorm, -16 LUFS) and normalized ALL 733 → peak spread now **5.1 dB**, 0 broken. `because` and everything else now play at a consistent volume. (Note: `because` wasn't actually the loudest by peak — it's a perceived-loudness effect loudnorm fixes.)
- **2026-07-08 (act-017)** — Owner added the OpenAI key + specified `gpt-4o-mini-tts`, voice **`sage`**, instructions "very friendly and funny, kids communication oriented (for 6 y.o. girl auditory)". Updated `scripts/generate_audio.py` accordingly (steerable model + `instructions`) and **generated all 733 clips** into `static/audio/` (19 MB, 0 missing). Browser-verified `/static/audio/cat.mp3`→200 audio/mpeg. TBD-008 closed, T-014 done. The robotic voice is gone — dictation now uses the natural `sage` kid voice (browser TTS still the fallback for any gap).
- **2026-07-08 (act-016)** — Wired the nicer-dictation seam (ADR-013): `speech.js` now prefers a pre-generated `/static/audio/<word>.mp3` and falls back to browser TTS when missing (**browser-verified**: 404→fallback, no errors). Built `scripts/generate_audio.py` (OpenAI TTS `tts-1`, idempotent/resumable) + ADR-013 + third-party-services entry + `.env.example` (`OPENAI_API_KEY`). Audio generation is gated on the owner's OpenAI key (TBD-008) — the app works meanwhile on the fallback voice. This is the last of the two owner decisions (the other, AI enrichment, shipped act-015).
- **2026-07-08 (act-015)** — AI enrichment ("AI enriches, engine leads"): built `agent/enrich.py` (DeepSeek → validated phrase/sentence/emoji per word; on-topic + length checks; no PII) + `scripts/enrich_wordbanks.py` (resumable batch, fills only missing fields, atomic write). Sample-verified quality (cat→🐱 "The cat naps now."), then ran full: **707/708 words enriched** (1 failed validation, safely left null). Cross-authored 38 enrich tests → 556 total green, 100% enrich.py. Next: OpenAI-TTS pre-generated audio (T-014, needs owner's OpenAI key).
- **2026-07-08 (act-014)** — Grew the corpus from 28 → **736 words / 9 patterns**. Sourced real word lists online (cvc-words.com, literacylearn, weareteachers, phonics corpora) via WebSearch/WebFetch; assembled `scripts/raw_words.py`; built `engine/phonics.py` (grapheme segmenter + 1–5 **difficulty** score — answers "which word is harder") and `scripts/build_wordbanks.py` (transforms raw→schema, MERGES existing hand-crafted emoji/sentences, computes phonemes/difficulty/distractors). Wired **difficulty-aware selection** (`selector.target_difficulty` + `_pick_word(target=)`): low mastery→easy words, high→hard; warm-ups always easiest. Owner decisions recorded (2026-07-08): dictation → **pre-generated OpenAI TTS** (T-014); **"AI enriches, engine leads"** → AI generates phrases/sentences/emoji + refines difficulty (T-015), and may vary words within the engine's chosen skill+difficulty (T-016). Cross-authored phonics/selector/integrity tests in progress. Updated a stale word-order test (mechanical).
- **2026-07-07 (act-013)** — Built the **parent dashboard** (`/parent`, `engine/report.py`): server-rendered rose-of-winds SVG radar (14 axes from effective mastery), "work on next" weakest-three, recent-errors log (parent-only — target/attempt/tags/skill), and a settings form (items/session + voice rate, clamped) → `profile.json`. `templates/parent.html` + `static/css/parent.css`, tokens-only, zero inline styles. Browser-verified against real session data (radar shows the spiky mastery shape; error log shows correct classifier tags). Fixed a dark-mode bug (no body background → dark-on-dark) by setting `body` bg on both parent + kid pages. Updated the stale `/parent` placeholder test (mechanical). Cross-authored report tests in progress. Satisfies T-003 for v1.
- **2026-07-07 (act-012)** — **Phase-1 core complete & playable.** B8: `agent/teacher.py` (kid-voice correction feedback, PII-safe prompt per invariant #2, `SPELLQUEST_AGENT_LIVE` gate, engine fallback) wired into the correction routine + a teacher-notebook note on finish; `agent/client.py` (DeepSeek wrapper, certifi TLS). B7: adopted the design tokens/components into `static/css/`, built the vanilla-JS SPA (`main.js` router + `registry`/`exercises.js` 3 renderers + `correction.js` overlay + `speech.js` TTS seam + `api.js`), `templates/app.html`, `/app` route. Sonnet cross-authored 48 agent tests → 312 total, 100% on agent modules. **Browser-verified** the full loop (home→teach→word_builder→correction showing the correct word→reward ⭐10); all 3 renderers confirmed. Caught + fixed a real bug: Flask debug reloader restarts the server on `data/` writes (would wipe a session mid-answer) → `use_reloader=False`. Deferred JS unit tests (T-010, needs a JS harness) + logged polish tickets (T-011..T-013). Two commits: B8 agent, B7 UI.
- **2026-07-07 (act-011)** — Built the session layer (lead-authored): `engine/contracts.py` (ADR-005 — item envelope, per-type payloads for the 3 Phase-1 exercises, `validate_item`, `public_item` that strips answer keys, `tool_schemas()` for the agent), `engine/session.py` (ADR-009 — start/resume/get/answer/finish, server-authoritative grading, correction routine, step-down on 2 first-try misses, mastery persistence, session logs, reward/hatch application), `store.load_session_log`, and `/api/session/{start,item,answer,finish}` + `/api/skills`. Lead smoke-caught + fixed a focus-skill-without-content bug. Sonnet cross-authored 79 tests → **lead-verified**: 264 pass, 100% on contracts.py + session.py (app 98%). Judged 2 findings: clarified step-down semantics (consecutive = first-try misses) in code + ADR-007; the `_apply_mastery` defensive no-op is intended. De-risked B8: built `agent/client.py` and verified live DeepSeek function-calling returns proper tool_calls for the contract schemas (needed `certifi` for macOS TLS). `.claude/launch.json` added for preview. Session slice committed (agent/ + certifi held for the B8 commit).
- **2026-07-07 (act-010)** — Built the deterministic engine brain (lead-authored): `engine/skills.py` (EMA mastery, lazy-projection decay, one-way intro latch, ADR-007), `engine/classifier.py` (Levenshtein alignment + frozen 9-tag taxonomy, phoneme-aware phonetic detection, ADR-008), `engine/selector.py` (60/30/10 + warm-up + end-on-win, deterministic via seeded rng, ADR-007), `engine/rewards.py` (add-only stars/levels + streak-freeze SM + sustained hatching, ADR-011). Expanded word banks to 3 (short_vowels 8, digraphs 10, heart_words 10). Lead smoke-caught + fixed 2 impl bugs before cross-author handoff (classifier ignored phoneme data → luv/love now flagged; selector truncated 2 items when prepending warm-ups → sizing fixed). Sonnet cross-authored 103 tests → **lead-verified**: 185 pass, 100% cov on ALL engine modules (app 96%, `__main__` guard). Judged 3 findings: fixed ADR-011 level-curve formula typo (code was right), marked 2 verified-unreachable defensive branches `# pragma: no cover`. Committed.
- **2026-07-07 (act-009)** — Accepted ADRs 005–012 on owner "proceed" (ADR-007 second-ideator pass fixed compounding-decay + premature-unlock). Built Wave 1 foundation: `engine/store.py` (atomic writes, `student_id`+traversal safety, tolerant reads, idempotent bootstrap — lead-authored, invariant #1), `engine/config.py`, `engine/models.py`, `app.py` skeleton (health + tenancy routes + factory), seed `data/word_bank/`. Lead self-verified via smoke run (caught a word-bank-in-temp-dir test gotcha). Sonnet cross-authored 82 tests → **verified by lead**: 82 pass, 99% cov (store/config/models 100%, app 96% — only the `__main__` guard uncovered). Judged the tests high-quality (real failure-path + guardrail assertions); accepted unchanged. 3 minor smells flagged by tester, all non-issues for v1. Untracked teacher-methodology PDF noticed in `docs/` — left for owner. Committed foundation + tests.
- **2026-07-07 (act-008)** — Lowered PLAN.md's altitude into 8 Proposed Stage-1 ADRs (005 exercise contract, 006 data model/store schema, 007 mastery+selection algorithm, 008 classifier alignment+taxonomy, 009 session lifecycle/state, 010 frontend app architecture, 011 rewards economy, 012 agent integration). Cross-referenced the design handoff's real tokens/components. Added the lock-order roadmap + accept-gates to adr/README.md and runner §B'; flagged 007/008/011 for owner pedagogy sign-off (and 007 for a 2nd-ideator pass). Docs-only; no code, so no tests/gates. All target Stage 1 only.
- **2026-07-07 (act-007)** — Owner declared Stage 2 (Mac/Android WebView wrappers + push, multi-user service with parent/kid accounts + billing, ~10k-user scale — later staged). Locked ADR-004: v1 stays single-user but keeps four seams clean (`student_id` on every store call, stateless API, tenancy-shaped routes, WebView-clean frontend + `speech.js` TTS wrapper). Parked TBD-005/006/007 with the stage gate as resurface trigger; PLAN §10 added; architecture.md updated. Docs-only act.
- **2026-07-07 (act-006)** — Created `.env` (owner pasted the real DeepSeek key) + committed `.env.example`. Verified `.env` is gitignored and probed the API (HTTP 200, "pong", 12 tokens — sub-cent spend per the de-risk rule). `deepseek-chat` alias currently serves `deepseek-v4-flash` → noted in third-party-services as a reason output validation is load-bearing. TBD-001 closed, runner C2 resolved, stub row removed (no marker ever hit code).
- **2026-07-07 (act-005)** — Wired the GitHub remote (`https://github.com/sabatico/train.git`), renamed branch main→master per owner instruction, pushed all history. Owner granted standing commit/push permission to `master` (no per-push approval) — recorded in CLAUDE.md §Git; destructive-action rule unchanged. Docs-only otherwise.
- **2026-07-07 (act-004)** — Received the Claude Design handoff (`design_handoff_spell_quest/`): kid screens + all 10 exercise cards, tokens.css + components.css + 2 `.dc.html` design references. Committed as-is (reference material); runner C1 resolved, B2/B7 unblocked, B10 opened for the missing parent screens + §8 states. Note: this landed mid-act and was briefly swept into the act-003 commit by an unscoped `git add -A`; history was split into two clean commits (no remote, safe).
- **2026-07-07 (act-003)** — Owner set the model/role policy: Sonnet 5 + DeepSeek as builders AND test authors (cross-authored, never same slice); Opus 4.8 as judge/QR/verifier; escalation ladder Sonnet/DeepSeek → Opus → Fable after 2 failed attempts; max context in every agent spawn. Recorded in CLAUDE.md §standing rules. Note: DeepSeek runs as agent only after its key + a wrapper script exist; until then cross-author = separate Sonnet instance. Docs-only act, no tests to run.
- **2026-07-07 (act-002)** — Adopted the dev_harness: instantiated CLAUDE.md/CONVENTIONS.md/CHEAT-SHEET.md, all running files, SOPs, CI-gates doc; locked ADR-001/002/003; updated PLAN.md to a single AI-from-start phase with DeepSeek; wrote DESIGN_BRIEF.md for Claude Design. No code yet, so no tests/gates to run. Initial commit.
- **2026-07-07 (act-001)** — Wrote `PLAN.md`: pedagogy (why her errors happen, OG principles), 14-skill rose-of-winds model, 10 exercise types, error classifier, architecture (Flask + file DB + AI agent), kid-UI rules, gamification. Owner corrections captured: one phase, AI from the start, DeepSeek APIs.

> **Maintenance rule:** append a §8 line and refresh §4/§5/§6 at the end of **every** act, before yielding. Stable sections (§1–§3, §7) change rarely.

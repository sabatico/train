# secrets — never committed (CLAUDE.md standing rule)
.env

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# macOS
.DS_Store

# child's runtime learning data — personal, not source (ADR-006 layout).
# data/word_bank/ IS source (curated content) and stays tracked; per-student data is not.
data/students/
data/tts_cache/

"""Spell Quest deterministic engine (ADR-001).

Everything pedagogical that must work without the AI agent lives here: storage,
the skill/mastery model, the adaptive selector, the error classifier, session
assembly, and rewards. The engine can run a complete session alone (ADR-002).
"""

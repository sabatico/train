# Spell Quest — runtime + dev deps. Kept deliberately small (CONVENTIONS: every dep is paid for).
Flask==3.1.3
python-dotenv==1.0.1
certifi==2024.7.4          # CA bundle for TLS to DeepSeek (macOS Python lacks a system one)

# dev / test
pytest==8.2.2
pytest-cov==5.0.0

# content tooling (build-time only, not imported by the app):
# bundled ffmpeg for loudness-normalizing the generated dictation audio.
imageio-ffmpeg==0.5.1
